{
  "name": "bb-cypress-test",
  "version": "1.0.0",
  "description": "Testing BrandBoom out on .us (possibly on .co and .com if applicable)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cy:group1": "cypress run --record --parallel --group group1 --ci-build-id hello --spec cypress/e2e/group1/**/*",
    "cy:group2": "cypress run --record --parallel --group group2 --ci-build-id hello1 --spec cypress/e2e/group2/**/*",
    "cy:group3": "cypress run --record --parallel --group group3 --ci-build-id hello2 --spec cypress/e2e/group3/**/*"
  },
  "author": "Kurt Ian Khalid Israel",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.14.2"
  },
  "dependencies": {
    "@faker-js/faker": "^9.0.2"
  }
}
